# Implementation projects

This repo can pull projects from GitHub automatically.

- Edit `data/site.json` → `githubUsername`
- Optionally list `implementationProjects.featuredRepos` to pin specific repos.

If you prefer full manual control:

- Set `implementationProjects.mode` to `manual`
- Fill `implementationProjects.manual` with entries.
